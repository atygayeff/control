{
  "name": "control",
  "version": "1.0.0",
  "description": "Control project from local repo",
  "main": "index.js",
  "scripts": {
    "compile": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Samat",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.4.3"
  }
}
